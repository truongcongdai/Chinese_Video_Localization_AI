yt-dlp>=2026.6.9
playwright>=1.40.0
python-dotenv>=1.0.1
demucs
# NumPy 2.4 lazy dtype loading recurses inside Accelerate under concurrent
# web workers. Use the CUDA wheel explicitly: installing these packages from
# PyPI alone can silently select a CPU-only torch even on an NVIDIA machine.
numpy>=1.26,<2.3
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128
torchaudio==2.11.0+cu128
# Local CPU/CUDA image generation for creator scenes (SD-Turbo).
diffusers>=0.39.0
transformers>=5.0.0
accelerate>=1.14.0
safetensors>=0.8.0
openai-whisper
edge-tts>=7.2.8
redis>=5.0.0
Flask>=3.0.0
requests>=2.31.0
python-telegram-bot>=21.0
googletrans>=4.0.0
librosa>=0.10.2
mutagen>=1.47.0
# Needed for LocalizationConfig.enable_text_cover (now default-enabled):
# OCR-based detection of burned-in on-screen text (e.g. Chinese subtitles
# baked into the video pixels) so they can be covered with a white box and
# replaced with the translated text at the same screen position/timing.
easyocr

# Web UI (scripts/run_web.py) — replaces the Telegram bot with a browser UI
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
itsdangerous>=2.1.2
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 (its last release) runs an internal self-test
# (detect_wrap_bug) against the installed bcrypt backend that is broken by
# bcrypt>=4.1 (bcrypt removed the `__about__` attribute passlib reads, and
# tightened long-password handling to raise instead of silently
# truncating) -- this makes even the FIRST call to hash_password()/
# verify_password() crash with "password cannot be longer than 72 bytes"
# regardless of the actual password length. Pin bcrypt below that line
# until passlib cuts a release with a fix.
bcrypt<4.1.0
